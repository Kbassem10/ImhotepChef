# =================================================================
# ImhotepChef Environment Configuration
# =================================================================
# Copy this file to .env and fill in your actual values
# IMPORTANT: Never commit your .env file to version control!
# =================================================================

# Django Secret Key (REQUIRED)
# Generate a new secret key for production: https://djecrety.ir/
SECRET_KEY='django-insecure-your-secret-key-here-change-me-in-production'

# =================================================================
# DATABASE CONFIGURATION (REQUIRED)
# =================================================================
# For local development, create a PostgreSQL database:
# sudo -u postgres createdb imhotepchef
# sudo -u postgres createuser imhotepchef_user

DATABASE_NAME='imhotepchef'
DATABASE_USER='imhotepchef_user'
DATABASE_PASSWORD='your_secure_password_here'
DATABASE_HOST='localhost'
DATABASE_PORT='5432'

# For Docker development, use these values:
# DATABASE_HOST='db'
# DATABASE_PORT='5432'

# =================================================================
# GOOGLE AUTHENTICATION (OPTIONAL)
# =================================================================
# Get these from: https://console.developers.google.com/
# 1. Create a new project or select existing
# 2. Enable Google+ API
# 3. Create OAuth 2.0 credentials
# 4. Add your domain to authorized origins

GOOGLE_CLIENT_ID='your-google-client-id.apps.googleusercontent.com'
GOOGLE_CLIENT_SECRET='your-google-client-secret'

# =================================================================
# EMAIL CONFIGURATION (OPTIONAL)
# =================================================================
# For sending emails (password reset, notifications, etc.)
# Use an app-specific password for Gmail

MAIL_PASSWORD='your-email-app-password'
MAIL_HOST='smtp.gmail.com'
MAIL_PORT='587'
MAIL_USE_TLS='True'
MAIL_FROM_EMAIL='your-email@gmail.com'

# =================================================================
# DEVELOPMENT SETTINGS
# =================================================================
# Set to False in production
DEBUG='True'

# Allowed hosts (comma-separated)
ALLOWED_HOSTS='localhost,127.0.0.1,0.0.0.0'

# =================================================================
# QUICK START FOR CONTRIBUTORS:
# =================================================================
# 1. Copy this file: cp .env.example .env
# 2. Update DATABASE_* values with your PostgreSQL credentials
# 3. Generate a new SECRET_KEY (optional for development)
# 4. Leave Google and Email settings empty for basic functionality
# 5. Run: python manage.py migrate
# 6. Run: python manage.py runserver
# =================================================================